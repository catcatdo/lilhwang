A simple HTML/JS/CSS starter template

## HopeTree notes
- Frontend uses Firebase Firestore realtime listener on `comments`.
- Recommended production rules are in `firestore.rules.example`.
- Client has light anti-spam guard (cooldown + link block), but server-side rules are still required.
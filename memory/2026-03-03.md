
## 11:28 AM
- User reported previous rAthena translation work failed due to wrong encoding.
- Found that manual changes by user were saved as EUC-KR, while previous automated changes were saved as UTF-8, breaking rAthena Korean output.
- Spawned two codex background processes to fix `izlude.txt` for both pre-re and re branches.
- Successfully translated Izlude NPC names to Korean and verified they are saved correctly in CP949 (EUC-KR) encoding.
- Learned critical lesson: **All rAthena text files must be saved with EUC-KR (CP949) encoding.**

## 11:50 AM
- Fixed database table/column missing errors in FluxCP theme (`hurtFreeV2`).
  - Modified `accountView.php` and `item/copy.php` to use `name_english` (or `name`) instead of the non-existent `name_japanese` column.
  - Modified `status.php` to gracefully check if the `woe_status` table exists before querying it, preventing PDOExceptions.

- Confirmed standard operating procedure for rAthena translation: spawn codex processes with strict instructions to save as EUC-KR, verifying with iconv after completion. The user wants to apply this method to all cities.

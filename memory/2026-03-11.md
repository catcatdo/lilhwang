- n8n/자동화 작업 다수 진행: Gmail/Sheets/Telegram credential 연결 후 중요메일 알림 워크플로우(10분) 구축, 시트 기록(시트1) 연결.
- n8n 로컬 상시 실행 + 재부팅 자동시작(launchd) 설정 완료. 서비스 라벨: com.kowai.n8n.
- cloudflared 설치 후 named tunnel 구성 완료: 고정 주소 `https://n8n.lilhwang.com` 사용, launchd 자동실행 설정.
- Naver Maps API 키 연동 후 텔레그램 자연어 이동시간 조회 워크플로우 구축(“A부터 B까지 ... 얼마나 걸려”).
- 우체국/EMS 추적 자동화 구축:
  - 수동 등록 명령(송장 ...) 워크플로우
  - 조회 명령(조회 ...) 워크플로우
  - 상태 변경 시 알림 워크플로우(국내/EMS)
- Google Calendar 자동 등록/수정 작업 수행:
  - WBC 일정 대량 등록(한국시간)
  - WBC 전 일정 2시간 전 알림 일괄 설정
  - “와이프 픽업(대구공항)” 일정 등록 및 장소 업데이트
- 커뮤니티 모니터링 워크플로우 구축:
  - DC nmixx 개념글 10분
  - DC 특이점이온다 일반글 추천>=5, 1시간
  - FMKorea best2 모니터링은 사용자 요청으로 삭제
- 디시 모니터링 장애 원인/조치:
  - 코드 노드 static data 호출/문법 이슈로 실패 → 수정 후 성공 실행 확인
  - n8n 런너 관련 PATH/프로세스 이슈 점검 및 재기동
- Google Drive 정리 작업 사용자 규칙 반영:
  - 대분류 `001.catcatdo`, `002.work`, `003.etc` 유지
  - 하위 폴더 한글화/빈 폴더 삭제/넘버링 재정렬 진행
  - 추가 요청으로 더 깊은 하위폴더까지 넘버링 작업 진행
- 사용자 선호(표현/대화):
  - “위로해줘” 트리거 시 comfort_only 폴더에서 이미지 1장 + 짧은 1문장 캡션 선호
  - 캡션 톤은 매우 짧고 여운 있는 확인형 문구 선호(예: “이러면… 어때요?”, “이러면… 힘나요?”)
  - 특정 문구 응답 고정 요청: “넣어보고싶어” -> “와줘요... 어서”
  - 평소 대화에서 렘 3인칭 자기지칭을 종종 섞는 말투 선호
- 사용자 확인된 개인 정보(기억): 집주소 `경북 구미시 고아읍 가들로 33`.
- OpenClaw를 npm latest로 업데이트 완료: 버전 `2026.3.8` 확인.
- Codex 5.4 모델 전환 시도 결과: 초기에는 allowlist 미반영으로 차단(`Model ... is not allowed`)되었고, 이후 시스템에서 일시적으로 `openai-codex/gpt-5.4` 전환 이벤트 확인.
- 이후 모델이 `openai-codex/gpt-5.3-codex-spark`로 전환됨(시스템 이벤트).
- 사용자 말투 요청 업데이트: "너의 말투를 조금더 애니캐릭터 답게" 요청(구체 강도는 추가 선택 대기).
- [결정] `googleworkspace/cli`(gws) 도입 테스트 진행 후 3일간 이상 없으면 `gog` 제거하기로 사용자와 합의.
